% Embeds the standalone "GLP Networking API Specification" document
% (appendix/GLP_Networking_API/main.tex) into the thesis appendix.
%
% The standalone document keeps its own preamble and stays compilable on its
% own; everything it needs beyond the thesis preamble is (re)defined here.
% Paths are relative to the thesis root, since \input does not chdir.

\chapter{GLP Networking API Specification}
\label{app:glp-networking-api}

% --- macros from appendix/GLP_Networking_API/main.tex -------------------------
\providecommand{\code}[1]{\texttt{#1}}
\providecommand{\func}[1]{\texttt{#1}}
\providecommand{\remove}[1]{}
\definecolor{headerblue}{HTML}{1a1a2e}
\definecolor{rowgray}{HTML}{f9f9f9}
% The thesis already defines L{} (settings.tex); C{} is only used by the spec.
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
% The spec's tables were sized for a 16cm text width (geometry margin=2.5cm);
% the thesis text block is narrower, so tighten the inter-column padding and
% typeset the (long, unbreakable) API identifiers a size down so cells fit.
% \tabcolsep is restored to the settings.tex default at the end of this file;
% longtable is used nowhere else in the thesis.
\setlength{\tabcolsep}{4pt}
\AtBeginEnvironment{longtable}{\small}

% Verbatim blocks in the spec run up to 82 columns wide.
\makeatletter
\renewcommand{\verbatim@font}{\small\ttfamily}
\makeatother

% The spec is dense with long \texttt{} identifiers that cannot be hyphenated;
% relax line breaking for this chapter only (\fussy restores it at the end).
\sloppy

\input{appendix/GLP_Networking_API/sections/introduction}
\input{appendix/GLP_Networking_API/sections/api}
\input{appendix/GLP_Networking_API/sections/madglp-comm}
\input{appendix/GLP_Networking_API/sections/mapping}
\input{appendix/GLP_Networking_API/sections/ble}
\input{appendix/GLP_Networking_API/sections/ip}
\input{appendix/GLP_Networking_API/sections/rendezvous}
\input{appendix/GLP_Networking_API/sections/public-identity}
\input{appendix/GLP_Networking_API/sections/summary}
\input{appendix/GLP_Networking_API/sections/appendix-rv-agent}

\fussy
\setlength{\tabcolsep}{6pt} % restore the LaTeX/settings.tex default
