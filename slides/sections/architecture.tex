\begin{frame}{Architecture}
    \centering
    \resizebox{\textwidth}{!}{
        \begin{tikzpicture}[
            stack/.style={
                draw=TUMBlue!70!black,
                thick,
                rounded corners=2pt,
                minimum height=0.54cm,
                font=\sffamily\large,
                align=center,
                outer sep=0pt,
                inner xsep=8pt,
                inner ysep=2pt,
                fill=white
            },
            shared/.style={
                stack,
                minimum width=11.6cm
            },
            transport/.style={
                stack,
                minimum width=5.4cm,
                fill=TUMBlue!4
            },
            subtransport/.style={
                stack,
                minimum width=2.55cm,
                fill=TUMBlue!2,
                font=\sffamily\normalsize
            },
            bearer/.style={
                stack,
                minimum width=2.45cm,
                fill=TUMDarkerGreen!5,
                font=\sffamily\normalsize
            },
            arrow/.style={
                ->,
                >=stealth,
                thick
            }
        ]

            \node[shared, fill=TUMBlue!7] (glp) at (0,5.35) {GLP};
            \node[shared, fill=gray!6] (gn) at (0,4.55) {GN (Grassroots Networking)};
            \node[shared, fill=TUMOrange!8] (noise) at (0,3.75) {Noise};

            \node[transport] (ble) at (-3.05,2.85) {BLE};
            \node[transport] (udx) at (3.05,2.85) {UDX};

            \node[subtransport] (central) at (-4.45,1.95) {Central\\scanning};
            \node[subtransport] (peripheral) at (-1.65,1.95) {Peripheral\\advertising};

            \node[transport, fill=TUMBlue!2] (udp) at (3.05,1.95) {UDP NAT traversal};
            \node[transport, fill=TUMBlue!2] (ip) at (3.05,1.05) {IP};

            \node[bearer] (wlan) at (1.65,0.15) {WLAN};
            \node[bearer] (cellular) at (4.45,0.15) {Cellular};

            \draw[arrow] (glp) -- (gn);
            \draw[arrow] (gn) -- (noise);

            \draw[arrow] (noise.south -| ble.north) -- (ble.north);
            \draw[arrow] (noise.south -| udx.north) -- (udx.north);

            \draw[arrow] (ble.south -| central.north) -- (central.north);
            \draw[arrow] (ble.south -| peripheral.north) -- (peripheral.north);

            \draw[arrow] (udx) -- (udp);
            \draw[arrow] (udp) -- (ip);
            \draw[arrow] (ip.south -| wlan.north) -- (wlan.north);
            \draw[arrow] (ip.south -| cellular.north) -- (cellular.north);

        \end{tikzpicture}
    }
    \vspace{0.2em}
    {\footnotesize
    \begin{itemize}
        \setlength\itemsep{0.05em}
        \item GN uses Noise to establish secure sessions over BLE or UDX.
        \item BLE operates through central scanning and peripheral advertising roles.
        \item UDX uses UDP for NAT traversal over IP, carried by WLAN or cellular.
    \end{itemize}
    }
\end{frame}
